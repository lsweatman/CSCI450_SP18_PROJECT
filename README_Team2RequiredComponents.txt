Open a command line interface and navigate to the project folder and run the following commands:

npm install --save react-navigation               //installs file dependencies for page navigations
npm i react-native-simple-radio-button --save     //installs file dependencies for radio buttons
